# ElaraReplClient

Generated by `elara-project-builder`.

Selected features:
- REPL client: yes
- Socket mode: disabled
- Thread pool: no
- Debug harness and artifacts: yes
- Threaded worker: no
- IndexedDataStore skeleton: no

Build steps:
1. `./build.sh`
2. `./debug.sh` to run validator tests with artifacts
3. `./stress.sh 30` to run timed stress tests with artifacts
4. `./fuzz.sh 30` to run timed fuzz tests with artifacts
5. `sudo ./install.sh` to install the already-built binary under `/usr/local`
6. `sudo ./install.sh --remove` to remove the installed binary
7. install state is tracked in `/usr/local/share/elara-repl-client/install-manifest.txt` by default

Build profiles:
- `make BUILD_PROFILE=release` for fast optimized builds
- `make BUILD_PROFILE=debug` for easier debugging
- `make BUILD_PROFILE=asan` for address/UB sanitizer runs
- `BUILD_PROFILE=debug ./build.sh` and `BUILD_PROFILE=debug ./install.sh` use the same profile via the helper scripts
- `install.sh` does not build; it expects artifacts from `./build.sh`
- `debug.sh`, `stress.sh`, and `fuzz.sh` create structured artifacts under `./artifacts`
- `make debug`, `make stress`, and `make fuzz` invoke the same generated test runner directly
- use `ARTIFACT_ROOT=/path ./debug.sh` to redirect artifacts elsewhere

Linting:
- `./build.sh` and `make` now run `elara.cpp-lint` before any compilation starts
- a lint failure blocks object compilation and linking entirely
- `make lint` runs the same strict lint pass explicitly against `./src`
- the generated project prefers `../build/bin/elara.cpp-lint` when it exists, then falls back to `/usr/local/bin/elara.cpp-lint`
- if you want to force the system install, use `ELARA_CPP_LINT=/usr/local/bin/elara.cpp-lint make lint`
- override this with `ELARA_CPP_LINT=/path/to/elara.cpp-lint make lint` if needed
- current policy allows primitives, safe Elara value types (`String`, `Memory`, `ByteArray`), plus `Ref`, `RefArray`, and `elara::threading::memory::Ref`
- permissable rules are expected to evolve over time as the framework policy is refined

By default the project uses Elara staged at `../build` for includes, libraries, and tools.
Override this with `ELARA_ROOT=/path/to/elara/build make` or `ELARA_ROOT=/usr/local make` if needed.
Use `ELARA_AGENT_API.md` as the local reference document for AI-driven edits and code generation.
